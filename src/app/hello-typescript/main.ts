let minhaVar = 'minha variável';

function minhaFunc(x: number , y: number) {
  return x + y;
}

let numeros = [1, 2 ,3];
numeros.map(x => x * 2);

class Matematica {
  soma(x: number, y: number) {
    return x + y
  }
}
